dist: xenial

language: python

python:
    - "3.7"

before_install:
    - pip install poetry

install:
    - poetry install

script:
    - poetry run flake8 app
    - poetry run mypy app
    - poetry run pytest

after_script:
    - pip install codecov
    - codecov

notifications:
  email: false
